 Plug 'tpope/vim-surround'



" Basically, this method can be run from any location on the line, to swap single quotes to double quotes and vice versa
" it uses c' because it's easier and does not require holding the shift key

command! SurroundRequote call surrounded#Requote()
nnoremap <leader>c' :SurroundRequote<cr>

 nmap <leader>qw ysiw"
 nmap <leader>QW ysiW"

 " vim:ft=vim
