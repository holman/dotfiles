eval "$(rbenv init -)"

if [ -f ~/.bashrc ]; then
  source ~/.bashrc
fi

# create ~/.localrc for settings you don't want committed
if [[ -a ~/.localrc ]]; then
  source ~/.localrc
fi
export PATH="/usr/local/opt/openssl/bin:$PATH"
