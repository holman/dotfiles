# Tap repositories
tap "homebrew/bundle"
tap "homebrew/core"

# Install packages
brew "fnm"
brew "pyenv"
brew "xz" #required by pyenv to install python
brew "pnpm"
brew "poetry"
brew "pipx"

# Install casks (for macOS users)
cask "visual-studio-code"
cask "ngrok"
cask "docker" #use the cask to install the docker-on-mac app
