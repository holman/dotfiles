# Tap repositories
tap "homebrew/bundle"
tap "homebrew/core"

# Install packages
brew "fnm"
brew "pyenv"
brew "xz" #required by pyenv to install python
brew "pnpm"

# Install casks (for macOS users)
cask "visual-studio-code"
