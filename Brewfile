
# set arguments for all 'brew install --cask' commands
cask_args appdir: "/Applications", require_sha: true

# productivity applications
#still need to check the require_sha: cask "google-chrome", args: { require_sha: false }

# media and socials
#still need to check the require_sha: cask "spotify", args: { require_sha: false}

# other
#still need to check the require_sha: cask "balenaetcher", args: { require_sha: false }

# useful tools
#require_sha check: cask "logi-options-plus", args: { require_sha: false }
#require_sha check: cask "fanny", args: { require_sha: false }

