#!/bin/bash -e

mkdir -p build
cd build
update=""
if [ $# -eq 1 ]; then
  update="--update"
fi
conan install $update --build=missing ..
if [[ -f ../conanfile.py ]]; then
  CONAN_CMAKE_GENERATOR=Ninja conan build -c ..
else
  cmake -G Ninja ..
fi
